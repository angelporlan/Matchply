{
  "name": "nextprof-promo-video",
  "version": "1.0.0",
  "description": "Video promocional para NextProf.AI con HyperFrames",
  "main": "index.html",
  "scripts": {
    "preview": "npx hyperframes preview",
    "lint": "npx hyperframes lint",
    "render": "npx hyperframes render --output promo-nextprof.mp4",
    "render:docker": "npx hyperframes render --docker --output promo-nextprof.mp4"
  },
  "dependencies": {},
  "devDependencies": {
    "hyperframes": "latest"
  }
}
